package org.poo.Minions;

/**
 * Enum-ul are rolul de a fi atribuit fiecarui minion in functie de tipul acestuia pentru
 * a putea fi identificata mai usor pozitionarea acestuia pe tabla de joc (in fata sau in spate).
 */
public enum Position {
    FRONT, BACK
}
